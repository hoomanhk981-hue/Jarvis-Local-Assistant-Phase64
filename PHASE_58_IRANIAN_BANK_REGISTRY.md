# Phase 58 — Iranian Bank Selection Registry

Adds a local registry for common Iranian bank profiles used by the banking UI.

Only stable IDs and display names are stored. No card numbers, CVV2, PINs,
OTPs, passwords, or API keys are stored. This phase does not execute payments.
